function accum(s) {
  let res = ''
  res += s[0].toUpperCase()
  let ind = 2
  for (let i = 1; i < s.length; i++){
    res += '-' + s[i].toUpperCase()
    res += s[i].repeat(i).toLowerCase()
  }
  return res
}
