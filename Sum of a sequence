const sequenceSum = (begin, end, step) => {
    let res = 0
    if (begin >= end) {
        return 0
    } else {
        do {
            res += begin
            begin  += step
        } while (begin <= end)


    }

return(res)
}
