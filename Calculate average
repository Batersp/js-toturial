function find_average(array) {
  let res = 0
  let sum = 0

  for (let i=0; i<array.length; i++) {
    sum += array[i]
  }
  res = sum / array.length
  
  if (array.length ==0) {
    res = 0
  }
    
  return res
  
}
